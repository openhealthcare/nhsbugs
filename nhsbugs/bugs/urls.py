from django.conf.urls.defaults import url, patterns

from django.views.generic.simple import redirect_to, direct_to_template


urlpatterns = patterns('',
#    url(r'^edit/$', view,name='profiles_edit_profile'),
)